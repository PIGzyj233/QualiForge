# Domain Docs

This repo uses a single-context domain documentation layout.

Agents should read:

- `CONTEXT.md` for project domain language and product concepts
- `docs/adr/` for architectural decision records
- `docs/mvp-prd.md` for MVP product scope
- `docs/future-roadmap.md` for deferred and future capabilities

There is no `CONTEXT-MAP.md` in the current layout.

